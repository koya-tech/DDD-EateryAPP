import { Meta, StoryObj } from '@storybook/react';
import ShareForm from './ShareForm';

const meta: Meta<typeof ShareForm> = {
    component: ShareForm,
};

export default meta;
type Story = StoryObj<typeof ShareForm>;

export const Primary: Story = {
};
