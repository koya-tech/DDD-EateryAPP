import { Meta, StoryObj } from '@storybook/react';
import DraggableMap from './LeafletMap';

const meta: Meta<typeof DraggableMap> = {
    component: DraggableMap,
};

export default meta;
type Story = StoryObj<typeof DraggableMap>;

export const Primary: Story = {
};
